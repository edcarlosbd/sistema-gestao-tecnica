# ===========================================
# CONFIGURAÇÃO DO BANCO DE DADOS
# ===========================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Configure suas credenciais do banco de dados
# 3. NUNCA commite o arquivo .env no Git!
#
# Comando: cp .env.example .env
#

# Host do banco de dados (geralmente localhost)
DB_HOST=localhost:3308

# Usuário do banco de dados
DB_USER=root

# Senha do banco de dados (deixe vazio se não tiver senha)
DB_PASS=

# Nome do banco de dados
DB_NAME=dbsisagendador

# Porta do MySQL (3306 é a padrão, 3308 em alguns XAMPP)
DB_PORT=3308

# ===========================================
# NOTAS IMPORTANTES
# ===========================================
# 
# - Para XAMPP/WAMP local, geralmente:
#   DB_HOST=localhost:3308 (ou localhost:3306)
#   DB_USER=root
#   DB_PASS= (vazio)
#
# - Para produção, SEMPRE use senhas fortes!
#
# - O arquivo .env está no .gitignore para
#   proteger suas credenciais
#
